Fix the current TypeScript build errors in the New National Advertising project.

IMPORTANT:
Do NOT redesign, refactor, or change unrelated functionality.
Do NOT modify Firebase authentication, Firestore schemas, API routes, frontend behavior, or deployment configuration unless absolutely required for this specific TypeScript error.

The current build error is in:

artifacts/api-server/src/routes/storage.ts

Current errors:

src/routes/index.ts:4:27 - error TS2306:
File '.../artifacts/api-server/src/routes/storage.ts' is not a module.

src/routes/storage.ts:
Cannot find name 'router'
Cannot find name 'requireAdmin'
Cannot find name 'RequestProductImageUploadUrlBody'
Cannot find name 'RequestProductImageUploadUrlResponse'
Cannot find name 'createFirebaseUploadTarget'
Parameter 'req' implicitly has an 'any' type
Parameter 'res' implicitly has an 'any' type

The storage.ts file should be a complete Express router module.

Restore the required imports and router declaration, including the equivalent of:

import { Router, type IRouter } from "express";
import {
  RequestProductImageUploadUrlBody,
  RequestProductImageUploadUrlResponse,
} from "@workspace/api-zod";
import { requireAdmin } from "./admin";
import {
  createFirebaseReadUrl,
  createFirebaseUploadTarget,
} from "../lib/firebase-storage";

const router: IRouter = Router();

Keep the existing:
POST /storage/uploads/request-url
GET /storage/objects/*path

Keep:
export default router;

The current request body type is:
{
  file: Blob;
  folder?: "products" | "machines" | "services" | "projects" | "requests";
}

Therefore do NOT access:
parsed.data.name
parsed.data.size
parsed.data.contentType

Use the Blob properties:
parsed.data.file.size
parsed.data.file.type

For the upload target's required name field, use a safe fixed value such as "upload" unless the existing schema/source provides a legitimate filename field. Do not invent a new schema.

After fixing storage.ts, run:

pnpm --filter @workspace/api-server exec tsc -p tsconfig.json --noEmit

Then run:

pnpm run build

If there are further errors, fix only errors directly caused by this issue and verify the build again.

Do not stop after editing the file. The final response must report the actual TypeScript/build result.